# Secrets — never commit
.env
.env.local
*.pem
cookies.json

# Establish The Run exports are paid subscriber content. The loader reads them
# from here; they are not ours to redistribute.
data/manual/

# The real league config names the user's actual leagues; the repo is public.
config/leagues.toml

# Data corpus: large, append-only, and personal. Rebuildable forward only.
/data/
!/data/.gitkeep

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
dist/
build/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Node / frontend
node_modules/
web/dist/
web/.vite/

# OS / editor
.DS_Store
.idea/
.vscode/
